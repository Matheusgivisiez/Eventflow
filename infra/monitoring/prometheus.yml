global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: eventflow-api
    metrics_path: /api/metrics
    static_configs:
      - targets: ["api:3001"]
  - job_name: rabbitmq
    static_configs:
      - targets: ["rabbitmq:15672"]
